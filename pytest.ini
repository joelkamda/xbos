[pytest]
testpaths = tests
python_files = test_*.py
addopts = -ra --strict-markers
markers =
    characterization: locks current legacy behavior
    integration: requires the local PostgreSQL test database