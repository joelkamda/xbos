[pytest]
testpaths = tests
python_files = test_*.py
addopts = -ra --strict-markers
markers =
    characterization: records current WND behavior as discovery and transformation evidence, not as a neutral-kernel contract
    contract: validates neutral-kernel contracts without application or database dependencies
    integration: requires the local PostgreSQL test database